<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <title>{% block title %} FMPS {% endblock %}</title>
        {% block stylesheets %}
           <link href="{{ asset('bundles/acmefmps/css/main.css') }}" rel="stylesheet" type="text/css" />
        {% endblock %}
        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div id="main">
            <div id="content">
            <div id="notice">
                {% if app.session.flash('notice') %}
                    <div class="flash notice">
                        {{ app.session.flash('notice') }}
                    </div>
                {% endif %}

                {% if app.session.flash('error') %}
                    <div class="flash error">
                        {{ app.session.flash('error') }}
                    </div>
                {% endif %}
           </div>
           
            {% block body %}{% endblock %}
            </div>
            <div id="footer">&copy; 2012 Fondation Marocaine pour la Promotion de l'enseignement préScolaire</div>
                
        </div>
    </body>
</html>