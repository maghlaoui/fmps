{% extends '::base.html.twig' %}

{% block body %}
<h3>ActualiteRubrique edit</h3>

<form action="{{ path('actualiterubrique_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('actualiterubrique') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('actualiterubrique_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}