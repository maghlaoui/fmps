{% extends '::base.html.twig' %}

{% block body %}
<h3>Editer une Actualité</h3>

<form action="{{ path('actualite_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Editer</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('actualite') }}">
            <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
    <li>
        
    </li>
</ul>
{% endblock %}