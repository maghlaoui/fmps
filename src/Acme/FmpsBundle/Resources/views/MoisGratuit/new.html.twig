{% extends '::base.html.twig' %}

{% block body %}
<h3>Ajouter un mois gratuit</h3>

<form action="{{ path('moisgratuit_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Ajouter</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('moisgratuit') }}">
            <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
</ul>
{% endblock %}