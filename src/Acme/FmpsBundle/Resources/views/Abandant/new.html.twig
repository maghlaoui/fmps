{% extends '::base.html.twig' %}

{% block body %}
<h3>Ajouter un abandant</h3>

<form action="{{ path('abandant_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Créer</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('abandant') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}