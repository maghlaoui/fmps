{% extends '::base.html.twig' %}

{% block body %}
<h3>CompteBancaire creation</h3>

<form action="{{ path('comptebancaire_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('comptebancaire') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}