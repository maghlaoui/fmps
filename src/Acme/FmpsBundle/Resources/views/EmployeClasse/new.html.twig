{% extends '::base.html.twig' %}

{% block body %}
<h3>Ajouter une affectation</h3>

<form action="{{ path('employeclasse_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Créer</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('employeclasse') }}">
            <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
</ul>
{% endblock %}