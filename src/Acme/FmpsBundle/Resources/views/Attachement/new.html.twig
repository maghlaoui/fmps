{% extends '::base.html.twig' %}

{% block body %}
<h3>Attachement creation</h3>

<form action="{{ path('attachement_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('attachement') }}">
          <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
</ul>
{% endblock %}