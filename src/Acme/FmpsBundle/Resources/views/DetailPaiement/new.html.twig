{% extends '::base.html.twig' %}

{% block body %}
<h3>DetailPaiement creation</h3>

<form action="{{ path('detailpaiement_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('detailpaiement') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}