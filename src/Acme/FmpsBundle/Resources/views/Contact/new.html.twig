{% extends '::base.html.twig' %}

{% block body %}
<h3>Ajouter un contact</h3>

<form action="{{ path('contact_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <input type="hidden" value="{% if partenaire_id is defined %}{{partenaire_id}}{% endif%}" name="partenaire_id" />
    <p>
        <button type="submit" class="btn">Créer</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('contact') }}">
            <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
</ul>
{% endblock %}