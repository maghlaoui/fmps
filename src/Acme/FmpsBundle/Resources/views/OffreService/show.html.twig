{% extends '::base.html.twig' %}

{% block body %}
<h3>Offre de service</h3>

<table class="table table-hover">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Ecole</th>
            <td>{{ entity.ecole }}</td>
        </tr>
        <tr>
            <th>Anneé scolaire</th>
            <td>{{ entity.anneeScolaire }}</td>
        </tr>
        <tr>
            <th>Service</th>
            <td>{{ entity.service }}</td>
        </tr>
				<tr>
            <th>Catégorie</th>
            <td>{{ entity.category }}</td>
        </tr>
        <tr>
            <th>Montant</th>
            <td>{{ entity.montantService }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('offreservice') }}">
            <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
    <li>
        <a href="{{ path('offreservice_edit', { 'id': entity.id }) }}">
            <span class="edit">Editer</span>
        </a>
    </li>
    <li>
        <form action="{{ path('offreservice_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn btn-danger" data-confirm="êtes vous sûre de vouloir supprimer?"><i class="icon-trash icon-white"></i> Supprimer</button>
        </form>
    </li>
</ul>
{% endblock %}