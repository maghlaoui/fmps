{% extends '::base.html.twig' %}

{% block body %}
<h3>Ajouter une ville</h3>

<form action="{{ path('ville_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal well">
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Créer</button>
    </p>
		<p class="required_notes">Les champs indiqués par un astérisque sont obligatoires.</p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('ville') }}">
            <span class="back">Retour &agrave; la liste</span>
        </a>
    </li>
</ul>
{% endblock %}